'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // let data = [];
    // user = {};
    // user.user_id = 'park';
    // user.email = 'park@naver.com';
    // user.password = '1324';
    // user.created_at = '2018-06-12 16:05:28.078';
    // data.push(user);
    // return queryInterface.bulkInsert('Users', data, {});
  },
  down: async (queryInterface, Sequelize) => {},
};
